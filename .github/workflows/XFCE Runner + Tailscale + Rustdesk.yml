name: Setup XFCE Runner + Tailscale + Rustdesk

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  setup-gui-runner:
    runs-on: ubuntu-latest
    env:
      TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
      USERNAME: runner
      PASSWORD: runner
      TAILSCALE_HOSTNAME: gh-xfce-${{ github.run_id }}

    steps:
      - uses: actions/checkout@v4

      - name: Ensure runner user has sudo
        run: |
          echo "${USERNAME}:${PASSWORD}" | sudo chpasswd
          echo "${USERNAME} ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/${USERNAME}

      - name: Install XFCE
        run: |
          echo "Installing lightweight XFCE Desktop..."
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq xfce4 xfce4-goodies xorg dbus-x11

          echo "startxfce4" | sudo tee /home/${USERNAME}/.xsession
          sudo chown ${USERNAME}:${USERNAME} /home/${USERNAME}/.xsession

      - name: Install RustDesk
        run: |
          echo "Installing RustDesk"
          CLIENT_URL="https://github.com/rustdesk/rustdesk/releases/download/1.3.1/rustdesk-1.3.1-x86_64.deb"
          CLIENT_DEB="/tmp/rustdesk-client.deb"

          wget -O "$CLIENT_DEB" "$CLIENT_URL"
          sudo dpkg -i "$CLIENT_DEB" || sudo apt-get install -f -y

          mkdir -p ~/.config/rustdesk

          rustdesk --headless --password "runner" &

          sleep 5

          CLIENT_ID=$(rustdesk --get-id | tr -d '[:space:]')
          echo "RUSTDESK_CLIENT_ID=$CLIENT_ID" >> "$GITHUB_ENV"

      - name: Install Tailscale + start rustdesk
        run: |
          echo "Installing Tailscale..."
          curl -fsSL https://tailscale.com/install.sh | sudo sh
          sudo tailscale up --authkey "${TAILSCALE_AUTHKEY}" --hostname "${TAILSCALE_HOSTNAME}"
          sudo tailscale ip -4

          sudo systemctl start rustdesk-hbbs
          sudo systemctl start rustdesk-hbbr

      - name: Connection Info + Keep Alive
        run: |
          echo "=== FINAL CONNECTION INFO ==="
          echo "USERNAME: ${USERNAME}"
          echo "PASSWORD: ${PASSWORD}"
          echo
          echo "Tailscale IPs:"
          sudo tailscale ip -4 || true
          echo
          echo "RustDesk Server Public Key:"
          echo "${RUSTDESK_SERVER_ID}"
          echo
          echo "RustDesk Client ID (use this to connect):"
          echo "${RUSTDESK_CLIENT_ID}"
          echo
          echo "Keeping runner alive..."
          while true; do
            sleep 600
          done
